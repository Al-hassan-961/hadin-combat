# ---------------------------------------------------------------------------
# HADIN-COMBAT – Python backend dependencies
# Copyright (c) 2026 Al-hassan Shehade & Dina Balcheh
# All rights reserved.
#
# NOTE FOR ANDROID / TERMUX USERS:
#   Do NOT rely on this file on Termux. pip must never build C extensions
#   there (NumPy's source build fails because Android's Bionic libc lacks
#   `ctanh`). Instead install Termux's pre-built binaries and use setup.py:
#
#       pkg install python-numpy python-opencv-python
#       pip install -e .
#
#   The `; sys_platform != 'android'` markers below exclude numpy/OpenCV on
#   Termux as a safety net. On Linux/macOS/Windows this file installs the
#   normal pre-built wheels. All remaining deps are pure-Python wheels (no
#   C source builds).
# ---------------------------------------------------------------------------
fastapi==0.111.0
# Plain uvicorn (no [standard] extras) to avoid compiling uvloop/httptools.
uvicorn==0.30.1
# numpy and OpenCV install from wheels on non-Android platforms. On Termux
# they come from `pkg` (pre-built):  pkg install python-numpy python-opencv-python
numpy>=1.24 ; sys_platform != 'android'
# Standard `opencv-python` binding (not headless) — the more complete and
# better-supported choice on desktops; Android uses the system package.
opencv-python>=4.8 ; sys_platform != 'android'
websockets==12.0
pybind11==2.12.0
python-dotenv==1.0.1
redis==5.0.4
python-multipart==0.0.9

# Optional (not required): improves pose quality when MediaPipe is available.
# mediapipe==0.10.14
