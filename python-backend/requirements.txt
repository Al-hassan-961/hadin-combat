# ---------------------------------------------------------------------------
# HADIN-COMBAT – Python backend dependencies
# Copyright (c) 2026 Al-hassan Shehade & Dina Balcheh
# All rights reserved.
#
# IMPORTANT: numpy and OpenCV are SYSTEM dependencies, not pip dependencies.
# They are NEVER listed here, so pip can never attempt a C source build
# (which fails on Termux/Android for Python 3.14: no ARM64 wheels, and the
# ninja `highway_qsort` build crash). Install them from your OS package
# manager instead:
#
#   Termux:     pkg install x11-repo tur-repo
#               pkg update
#               pkg install python-numpy python-opencv clang
#   Ubuntu:     sudo apt install python3-numpy python3-opencv
#   Desktop:    pip install -e ".[native]"   (convenience wheels)
#
# Everything below is a pure-Python wheel (no C compilation).
# ---------------------------------------------------------------------------
# Starlette (NOT FastAPI) — FastAPI needs pydantic-core (Rust) which has no
# ARM64 wheel for Python 3.14 and can't be built on Android. Starlette covers
# our routing/WebSocket/static needs with zero compiled dependencies.
starlette>=0.46,<2
# Plain uvicorn (no [standard] extras) to avoid compiling uvloop/httptools.
uvicorn==0.30.1
websockets==12.0
pybind11==2.12.0
python-dotenv==1.0.1
redis==5.0.4
python-multipart==0.0.9

# Optional (not required): improves pose quality when MediaPipe is available.
# mediapipe==0.10.14
